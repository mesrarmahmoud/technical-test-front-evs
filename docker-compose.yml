version: '3'

services:
  backend:
    build:
      context: ./backend
    ports:
      - "3000:3000"
  
  frontend:
    build:
      context: ./frontend
    ports:
      - "8080:8080"
    depends_on:
      - backend  # Ensure backend is started before frontend